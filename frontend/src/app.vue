<template>
  <!-- <div v-if="data">
    <h1>{{ data.title }}</h1>
    <p>{{ data.description }}</p>

    <div v-if="item">
      <h1>{{ item.title }}</h1>
    <p>{{ item.description }}</p>
    </div>
  </div> -->
  <nuxt-layout>
    <nuxt-page />
  </nuxt-layout>
</template>
